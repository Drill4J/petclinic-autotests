def junit4Version = System.getProperty("junit4Version") ?: '+'

dependencies {
    testImplementation group: 'junit', name: 'junit', version: junit4Version
}

test {
    useJUnit()
    systemProperties System.properties
}

